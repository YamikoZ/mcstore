Options -Indexes
RewriteEngine On

# ไฟล์จริง (CSS, JS, รูป) → ส่งตรง
RewriteCond %{REQUEST_FILENAME} !-f

# ยกเว้นเฉพาะ assets และ uploads เท่านั้น
RewriteCond %{REQUEST_URI} !/(assets|uploads)/ [NC]

# ทุกอย่างรวมถึงโฟลเดอร์ → เข้า index.php
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]
